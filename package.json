{
  "name": "denver-ebike-navigator",
  "version": "1.0.0",
  "description": "An application for e-bike riders in Denver to plan routes, check weather, and find charging stations.",
  "private": true,
  "scripts": {
    "start": "echo \"Please use a local web server to serve the project root directory.\"",
    "build-android": "npm i && npx cap add android && npx cap sync && npx cap open android",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/cli": "^6.1.0",
    "@capacitor/core": "^6.1.0"
  }
}
